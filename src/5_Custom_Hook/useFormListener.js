import { getElementType, getCheckedCheckboxValues } from "./utilities"

const useFormListener = (defaultValues = {}) => {
  // TODO: create an event listener, `onFormChange`,
  // that will augment the `formData` object
  // every time a change event occurs on a form
  // it should set the initial form to the defaultValues object
  // if it is provided
  return [{}, () => {}]
}

export default useFormListener
